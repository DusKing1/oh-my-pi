[workspace]
members = ["crates/*"]
resolver = "3"

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"
authors = ["Can Boluk"]
homepage = "https://omp.sh/"
repository = "https://github.com/can1357/omp"

# All dependencies live here; member crates reference them with
# `dep = { workspace = true }` and never pin their own versions.
[workspace.dependencies]
anyhow = "1.0"
arboard = { version = "3.6", features = ["wayland-data-control"] }
ast-grep-core = { version = "0.39", default-features = false, features = ["tree-sitter"] }
argmax = "0.3"
async-recursion = "1.1"
async-stream = "0.3"
async-trait = "0.1"
bigdecimal = "0.4"
binary-heap-plus = "0.5"
base64 = "0.22"
blake3 = "1.8"
block2 = "0.6.2"
bon = "3"
bstr = "1"
bytecount = { version = "0.6", features = ["runtime-dispatch-simd"] }
bytes = { version = "1.12.1", features = ["serde"] }
bytes-utils = { version = "0.1.4", features = ["serde"] }
cap-std = "4"
cached = "0.59"
candle-core = "0.11"
candle-nn = "0.11"
cfg-if = "1"
check_elevation = "0.2"
chrono = "0.4"
clap = { version = "4", features = ["derive", "wrap_help"] }
codesnake = "0.2"
compare = "0.1"
cookie = "0.18.2"
command-fds = "0.3"
crc32fast = "1.5"
dashmap = "6"
dns-lookup = "3"
faccess = "0.2"
fancy-regex = "0.17"
filetime = "0.2"
fastembed = { version = "5.17.4", default-features = false, features = ["hf-hub-rustls-tls", "ort-download-binaries-rustls-tls"] }
flate2 = { version = "1.1", default-features = false, features = ["rust_backend"] }
flume = "0.11"
foldhash = "0.2"
fs_extra = "1.3"
fs2 = "0.4"
futures = "0.3"
glob = "0.3"
globset = "0.4"
grep-cli = "0.1"
grep-matcher = "0.1"
grep-pcre2 = "0.1"
grep-printer = "0.3"
grep-regex = "0.1"
grep-searcher = "0.1"
heapless = { version = "0.9.3", features = ["serde"] }
hmac = "0.12"
hf-hub = { version = "1.0.0", default-features = false, features = ["rustls-tls"] }
hifijson = "0.2"
hostname = "0.4"
http = "1.5"
http-body-util = "0.1"
hyper = { version = "1.11", features = ["client", "http1", "http2", "server"] }
hyper-rustls = { version = "0.27", default-features = false, features = ["http1", "http2", "ring", "webpki-tokio"] }
hyper-util = "0.1"
ignore = "0.4"
indicatif = "0.18"
indenter = "0.3"
inherent = "1"
insta = { version = "1.47", features = ["glob", "ron", "redactions"] }
itertools = "0.14"
jaq-core = "2.1"
jaq-json = "1.1"
jaq-std = "2.1"
jiff = { version = "0.2", features = ["tzdb-bundle-platform"] }
layout-rs = "0.1.3"
libc = "0.2"
llama-cpp-2 = { version = "0.1.154", default-features = false }
lscolors = { version = "0.21", default-features = false, features = ["gnu_legacy"] }
memchr = "2"
memmap2 = "0.9"
mermaid-text = "0.57"
multer = "3.1"
nix = { version = "0.31", features = ["fs", "poll", "process", "resource", "signal", "term", "user"] }
nom = "8"
normalize-path = "0.2"
objc2-device-check = "0.3.2"
objc2-foundation = "0.3.2"
omp-llm = { path = "crates/llm", version = "0.1.0" }
omp-llm-broker = { path = "crates/llm-broker", version = "0.1.0" }
omp-llm-gateway = { path = "crates/llm-gateway", version = "0.1.0" }
omp-llm-anthropic = { path = "crates/llm-anthropic", version = "0.1.0" }
omp-llm-bedrock = { path = "crates/llm-bedrock", version = "0.1.0" }
omp-llm-catalog = { path = "crates/llm-catalog", version = "0.1.0" }
omp-llm-cursor = { path = "crates/llm-cursor", version = "0.1.0" }
omp-llm-devin = { path = "crates/llm-devin", version = "0.1.0" }
omp-llm-dialect = { path = "crates/llm-dialect", version = "0.1.0" }
omp-llm-egress = { path = "crates/llm-egress", version = "0.1.0" }
omp-llm-google = { path = "crates/llm-google", version = "0.1.0" }
omp-llm-gitlab = { path = "crates/llm-gitlab", version = "0.1.0" }
omp-llm-openai = { path = "crates/llm-openai", version = "0.1.0" }
omp-llm-ollama = { path = "crates/llm-ollama", version = "0.1.0" }
omp-llm-transport = { path = "crates/llm-transport", version = "0.1.0" }
omp-llm-types = { path = "crates/llm-types", version = "0.1.0" }
notify = { version = "=8.2.0", features = ["macos_kqueue"] }
num-bigint = "0.4"
num-traits = "0.2"
omp-llm-fm = { path = "crates/llm-fm", version = "0.1.0" }
omp-ast = { path = "crates/ast", version = "0.1.0" }
omp-core = { path = "crates/core", version = "0.1.0" }
omp-docserver = { path = "crates/docserver", version = "0.1.0" }
omp-hashline = { path = "crates/hashline", version = "0.1.0" }
omp-llm-tower = { path = "crates/llm-tower", version = "0.1.0" }
omp-llm-error = { path = "crates/llm-error", version = "0.1.0" }
omp-llm-local = { path = "crates/llm-local", version = "0.1.0" }
omp-proto = { path = "crates/proto", version = "0.1.0" }
omp-py = { path = "crates/py", version = "0.1.0" }
omp-rpc = { path = "crates/rpc", version = "0.1.0" }
omp-shell = { path = "crates/shell", version = "0.1.0" }
omp-shell-builtins = { path = "crates/shell-builtins", version = "0.1.0" }
omp-shell-engine = { path = "crates/shell-engine", version = "0.1.0" }
omp-slopjson = { path = "crates/slopjson", version = "0.1.0" }
omp-storage = { path = "crates/storage", version = "0.1.0" }
omp-telemetry = { path = "crates/telemetry", version = "0.1.0" }
omp-tui-macros = { path = "crates/tui-macros", version = "0.1.0" }
omp-voice-kokoro = { path = "crates/voice-kokoro", version = "0.1.0" }
omp-walker = { path = "crates/walker", version = "0.1.0" }
omp-ar = { path = "crates/ar", version = "0.1.0" }
onig = { version = "6", default-features = false }
ort = { version = "=2.0.0-rc.13", default-features = false }
opentelemetry = "0.32.0"
opentelemetry-appender-tracing = "0.32.0"
opentelemetry-otlp = { version = "0.32.0", default-features = false, features = ["http-proto", "logs", "metrics", "reqwest-blocking-client", "trace"] }
opentelemetry-semantic-conventions = "0.32.1"
opentelemetry_sdk = { version = "0.32.1", features = ["logs", "metrics", "rt-tokio", "trace"] }
os_display = "0.1"
parking_lot = "0.12"
parking_lot_core = "0.9.12"
parse_datetime = "0.14"
peg = "0.8"
pin-project-lite = "0.2"
platform-info = "2"
png = "0.18"
postcard = { version = "1.1.3", features = ["alloc"] }
pretty_assertions = "1"
proc-macro2 = "1"
proptest = "1.11.0"
prost = "0.14.4"
prost-build = "0.14.4"
prost-types = "0.14.4"
protox = "0.9.1"
pyo3 = "0.29.2"
quote = "1"
rand = { version = "0.10", features = ["std_rng"] }
rayon = "1.10"
regex = "1.11"
rlimit = "0.11"
rustc-hash = "2"
rustix = { version = "1.1", features = ["fs", "process", "time"] }
rusqlite = { version = "0.40", features = ["bundled", "fallible_uint"] }
ring = "0.17"
rustls = { version = "0.23", default-features = false, features = ["ring", "std", "tls12"] }
self_cell = "1"
serde = { version = "1.0.229", features = ["derive", "rc"] }
serde_json = { version = "1.0.151", features = ["preserve_order", "raw_value"] }
sha2 = "0.10"
sherpa-onnx = "1.13.4"
similar = "3"
smallvec = { version = "2.0.0-alpha.12", features = ["serde"] }
smol_bitmap = { version = "0.3.2", features = ["serde"] }
smol_str = "0.3.6"
strum = { version = "0.28", features = ["derive"] }
syn = { version = "2", features = ["full"] }
syntect = { version = "5.3", default-features = false, features = ["default-syntaxes", "regex-fancy", "yaml-load"] }
tempfile = "3"
terminfo = "0.9"
thiserror = "2.0.20"
tiktoken-rs = "0.11"
tokio = { version = "1", features = ["fs", "io-std", "io-util", "macros", "net", "process", "rt", "rt-multi-thread", "signal", "sync", "time"] }
tokio-stream = { version = "0.1", features = ["net"] }
tokio-tungstenite = { version = "0.28", default-features = false, features = ["connect", "handshake", "rustls-tls-webpki-roots"] }
tokio-util = "0.7"
tokio-rustls = { version = "0.26", default-features = false, features = ["ring", "tls12"] }
toml = "1.1"
tonic = "0.14.6"
tonic-health = "0.14.6"
tonic-prost = "0.14.6"
tonic-prost-build = "0.14.6"
tower = { version = "0.5", default-features = false, features = ["util"] }
tracing = "0.1"
tree-sitter = "0.25"
tree-sitter-astro = { version = "0.1.1", package = "tree-sitter-astro-next" }
tree-sitter-bash = "0.25"
tree-sitter-c = "0.24"
tree-sitter-c-sharp = "0.23"
tree-sitter-clojure = "0.1"
tree-sitter-cmake = "0.7.1"
tree-sitter-cpp = "0.23"
tree-sitter-css = "0.25"
tree-sitter-dart = "0.2"
tree-sitter-diff = "0.1"
tree-sitter-dockerfile = { version = "0.2.0", package = "tree-sitter-dockerfile-updated" }
tree-sitter-elixir = "0.3"
tree-sitter-elisp = "1.6.1"
tree-sitter-erlang = "0.16.0"
tree-sitter-fortran = "0.6.0"
tree-sitter-go = "0.25"
tree-sitter-graphql = "0.1.0"
tree-sitter-haskell = "0.23"
tree-sitter-hcl = "1.1"
tree-sitter-html = "0.23"
tree-sitter-ini = "1.4.0"
tree-sitter-java = "0.23"
tree-sitter-javascript = "0.25"
tree-sitter-json = "0.24"
tree-sitter-julia = "0.23"
tree-sitter-just = "0.2.0"
tree-sitter-kotlin = { version = "0.4", package = "tree-sitter-kotlin-sg" }
tree-sitter-lua = "0.5"
tree-sitter-make = "1.1"
tree-sitter-md = "0.5"
tree-sitter-nix = "0.3"
tree-sitter-objc = "3.0"
tree-sitter-ocaml = "0.24.2"
tree-sitter-odin = "1.3"
tree-sitter-php = "0.24"
tree-sitter-powershell = "0.26.4"
tree-sitter-proto = "0.4.0"
tree-sitter-python = "0.25"
tree-sitter-r = "1.2.0"
tree-sitter-regex = "0.25"
tree-sitter-ruby = "0.23"
tree-sitter-rust = "0.24"
tree-sitter-scala = "0.26"
tree-sitter-solidity = "1.2"
tree-sitter-sql = { version = "0.3.11", package = "tree-sitter-sequel" }
tree-sitter-starlark = "1.3"
tree-sitter-svelte = { version = "0.1.1", package = "tree-sitter-svelte-next" }
tree-sitter-swift = "0.7"
tree-sitter-tlaplus = "1.5"
tree-sitter-toml-ng = "0.7"
tree-sitter-typescript = "0.23"
tree-sitter-verilog = "1.0"
tree-sitter-vue = { version = "0.1.0", package = "tree-sitter-vue-next" }
tree-sitter-xml = "0.7"
tree-sitter-yaml = "0.7"
tree-sitter-zig = "1.1"
url = "2.5"
uucore = { version = "0.8", default-features = false, features = ["backup-control", "checksum", "colors", "encoding", "entries", "extendedbigdecimal", "fast-inc", "format", "fs", "fsext", "hardware", "libc", "lines", "mode", "parser", "pipes", "process", "quoting-style", "ranges", "ringbuffer", "signals", "sum", "time", "version-cmp", "wide", "update-control"] }
ulid = "3.0"
uzers = "0.12"
webpki-roots = "1.0"
voice-g2p = "0.2.2"
whoami = "2"
wiremock = "0.6"
whisper-rs = { version = "0.16", default-features = false }
windows-sys = { version = "0.61", features = ["Wdk_Storage_FileSystem", "Win32_Foundation", "Win32_Networking_WinSock", "Win32_Security", "Win32_Security_Authorization", "Win32_Storage_FileSystem", "Win32_System_Console", "Win32_System_Diagnostics_ToolHelp", "Win32_System_IO", "Win32_System_Pipes", "Win32_System_SystemInformation", "Win32_System_Threading", "Win32_System_WindowsProgramming", "Win32_UI_WindowsAndMessaging"] }
zerocopy = { version = "0.8", features = ["derive"] }
zeroize = "1.9"
zstd = "0.13"
xutf = "1.1"
xxhash-rust = { version = "0.8", features = ["xxh32"] }

[workspace.lints.rust]
# ──────────────────────────────────────────────────────────────────────────────
# Rust Lint Levels
# ──────────────────────────────────────────────────────────────────────────────
mismatched_lifetime_syntaxes = "allow"
missing_docs = "warn"                  # Every public symbol documented

[workspace.lints.clippy]
# ──────────────────────────────────────────────────────────────────────────────
# Base Lint Levels
# ──────────────────────────────────────────────────────────────────────────────
all = { level = "warn", priority = -1 }
correctness = { level = "deny", priority = -1 }
nursery = { level = "warn", priority = -1 }
pedantic = { level = "warn", priority = -1 }
perf = { level = "warn", priority = -1 }
style = { level = "warn", priority = -1 }
suspicious = { level = "deny", priority = -1 }

# ──────────────────────────────────────────────────────────────────────────────
# Synchronization Policy
# ──────────────────────────────────────────────────────────────────────────────
disallowed_methods = "deny"
disallowed_types = "deny"

# ──────────────────────────────────────────────────────────────────────────────
# Meta: Lint Attributes
# ──────────────────────────────────────────────────────────────────────────────
allow_attributes_without_reason = "warn" # Enforce reason for all #[allow]

# ──────────────────────────────────────────────────────────────────────────────
# Safety & Unsafe Code
# ──────────────────────────────────────────────────────────────────────────────
borrow_as_ptr = "allow"
cast_ptr_alignment = "allow"
ptr_as_ptr = "allow"
ref_as_ptr = "allow"
undocumented_unsafe_blocks = "warn"
unsafe_derive_deserialize = "allow"

# ──────────────────────────────────────────────────────────────────────────────
# Numeric Casts & Conversions
# ──────────────────────────────────────────────────────────────────────────────
cast_lossless = "allow"            # u32 as u64 - 'as' is cleaner than From
cast_possible_truncation = "allow" # u64 as u32 - often intentional
cast_possible_wrap = "allow"       # u32 as i32 - can be intentional
cast_precision_loss = "allow"      # f64 as f32 - sometimes acceptable
cast_sign_loss = "allow"           # i32 as u32 - sometimes needed
tuple_array_conversions = "allow"  # Non-Into conversion is more clear

# ──────────────────────────────────────────────────────────────────────────────
# Floating Point
# ──────────────────────────────────────────────────────────────────────────────
float_cmp = "allow" # Tests mostly do this + we know what we're doing

# ──────────────────────────────────────────────────────────────────────────────
# Functions & Closures
# ──────────────────────────────────────────────────────────────────────────────
inline_always = "allow"                      # Needed for performance-critical code
must_use_candidate = "allow"                 # Not every function needs #[must_use]
needless_pass_by_value = "allow"
redundant_closure_for_method_calls = "allow" # .map(ToString::to_string) can be clearer
return_self_not_must_use = "allow"           # Builder pattern methods

# ──────────────────────────────────────────────────────────────────────────────
# Structs & Types
# ──────────────────────────────────────────────────────────────────────────────
inconsistent_struct_constructor = "allow"
missing_fields_in_debug = "allow"         # Not every field needs to be in Debug output
struct_excessive_bools = "allow"          # Usually with builders

# ──────────────────────────────────────────────────────────────────────────────
# Pattern Matching
# ──────────────────────────────────────────────────────────────────────────────
match_same_arms = "allow"                    # Can be intentional for clarity
match_wildcard_for_single_variants = "allow" # _ can be clearer than listing variants
option_if_let_else = "allow"                 # match/if-let-else often clearer

# ──────────────────────────────────────────────────────────────────────────────
# Imports & Module Organization
# ──────────────────────────────────────────────────────────────────────────────
enum_glob_use = "allow"
items_after_statements = "allow" # Sometimes more readable
wildcard_imports = "allow"       # Cleaner for preludes and test modules

# ──────────────────────────────────────────────────────────────────────────────
# Variables & Type Inference
# ──────────────────────────────────────────────────────────────────────────────
let_underscore_untyped = "allow" # Type obvious from context
similar_names = "allow"          # 'req' and 'res' together are fine

# ──────────────────────────────────────────────────────────────────────────────
# Literals & Formatting
# ──────────────────────────────────────────────────────────────────────────────
tabs_in_doc_comments = "allow" # rustfmt (hard_tabs + format_code_in_doc_comments) emits them
unreadable_literal = "allow"   # 10_000_000 vs 0xDEADBEEF is context dependent
verbose_bit_mask = "allow"     # Explicit bit patterns can be clearer than hex

# ──────────────────────────────────────────────────────────────────────────────
# Code Style
# ──────────────────────────────────────────────────────────────────────────────
default_trait_access = "allow"        # Default::default() sometimes clearer
significant_drop_tightening = "allow" # We pay attention to this already

# ──────────────────────────────────────────────────────────────────────────────
# Documentation
# ──────────────────────────────────────────────────────────────────────────────
missing_errors_doc = "allow" # Documenting every error return is often redundant
missing_panics_doc = "allow" # Not every panic needs docs, especially assert!

# ──────────────────────────────────────────────────────────────────────────────
# Complexity
# ──────────────────────────────────────────────────────────────────────────────
too_many_arguments = "allow" # Argument count is rarely the real complexity signal
too_many_lines = "allow"     # Arbitrary limits don't account for necessary complexity

[profile.release]
opt-level = 2
debug = false
incremental = true
lto = "thin"
codegen-units = 1
strip = true
panic = "unwind"
