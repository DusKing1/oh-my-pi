{
  "cases": {
    "empty_stream": {
      "decoded": ["stop", "stop", "stop"],
      "retry": [500, 1000, "terminal"]
    },
    "empty_response": {
      "decoded": ["upstream", "upstream", "upstream"],
      "retry": [500, 1000, "terminal"]
    },
    "overload_recovery": {
      "decoded": ["overloaded", "stop"],
      "retry": [1000]
    },
    "context_overflow": {
      "decoded": ["upstream"]
    },
    "cancellation": {
      "retry": ["terminal"]
    }
  }
}
