// Deployment compat for provider "moonshot": transport/wire contract of this deployment,
// plus per-model residues the class stratum does not explain.

provider "moonshot" {
	models "kimi-k2-thinking" "kimi-k2.5" "kimi-k2.6" "kimi-k2.7-code" {
		thinking-efforts "minimal" "low" "medium" "high"
	}
}
