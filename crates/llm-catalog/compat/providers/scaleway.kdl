// Deployment compat for provider "scaleway": transport/wire contract of this deployment,
// plus per-model residues the family stratum does not explain.

provider "scaleway" {
	family "glm" {
		thinking-efforts "minimal" "low" "medium" "high" "max"
	}
}
