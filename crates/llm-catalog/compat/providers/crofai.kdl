// Deployment compat for provider "crofai": transport/wire contract of this deployment,
// plus per-model residues the class stratum does not explain.

provider "crofai" {
	class "kimi" {
		thinking-efforts "minimal" "low" "medium" "high" "xhigh"
	}
}
