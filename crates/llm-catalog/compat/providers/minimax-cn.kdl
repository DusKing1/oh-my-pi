// Deployment compat for provider "minimax-cn": transport/wire contract of this deployment,
// plus per-model residues the class stratum does not explain.

provider "minimax-cn" {
	models "MiniMax-M3" {
		thinking-efforts "low" "medium" "high"
	}
}
