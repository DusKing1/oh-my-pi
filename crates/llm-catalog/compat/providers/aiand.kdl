// Deployment compat for provider "aiand": transport/wire contract of this deployment,
// plus per-model residues the class stratum does not explain.

provider "aiand" {
	models "moonshotai/kimi-k2.7-code" {
		thinking-efforts "minimal" "low" "medium" "high" "xhigh"
	}
}
