// Deployment compat for provider "minimax": transport/wire contract of this deployment,
// plus per-model residues the family stratum does not explain.

provider "minimax" {
	models "MiniMax-M3" {
		thinking-efforts "low" "medium" "high"
	}
}
